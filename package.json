{
  "name": "geobork-client",
  "version": "1.0.0",
  "description": "geobork-client",
  "main": "index.js",
  "author": {
    "name": "Michael Miretski",
    "email": "4apaev@gmail.com"
  },
  "repository": {
    "type": "git"
  },
  "scripts": {
    "start" : "node index",
    "pug"   : "pug -P index.pug",
    "css"   : "stylus -c --sourcemap-inline css/style.styl",
    "cert"  : "cp ../*.pem ./conf",
    "dock"  : "docker-compose up -d --build",
    "build" : "npm run css && npm run pug && npm run dock"
  },
  "dependencies": {}
}
